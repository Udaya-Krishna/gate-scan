services:
  - type: web
    name: gate-scan-backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    region: oregon  # or choose your preferred region 